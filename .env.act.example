# Copy to `.env.act` and fill these values for local act testing
# Do NOT commit your real secrets. Add `.env.act` to .gitignore
GITHUB_TOKEN=ghp_local
# Add other secrets used by your workflows if needed, e.g.:
# VITE_FIREBASE_API_KEY=your_value
